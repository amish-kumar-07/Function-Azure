{
  "name": "website-crawler-function",
  "version": "1.0.0",
  "description": "Azure Function for crawling websites and detecting errors",
  "main": "index.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^24.17.0"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0"
  }
}
